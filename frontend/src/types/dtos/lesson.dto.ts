export type LessonPreviewDto = {
  id: string;
  name: string;
};

export type LessonDetailDto = {
  id: string;
  name: string;
  description: string;
  pdfDescription: string;
  pdfTitle: string;
  pdfUrl: string;
  videoDescription: string;
  videoTitle: string;
  videoUrl: string;
};
